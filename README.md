# AutoRegressionVM

VMware 가상 머신을 활용한 자동화된 회귀 테스트 도구입니다. GUI와 CLI 모드를 모두 지원하며, 스냅샷 기반의 반복 테스트 환경을 제공합니다.

## 주요 기능

- **VM 관리**: VMware Workstation VM 등록, 전원 상태 모니터링, 스냅샷 관리
- **스냅샷 기반 테스트**: 테스트 전/후 스냅샷으로 자동 복원하여 일관된 테스트 환경 보장
- **시나리오 기반 실행**: 여러 테스트 스텝을 시나리오로 구성하여 순차/병렬 실행
- **병렬 VM 실행**: 최대 N개 VM 동시 실행, VM별 독립적 예외 처리
- **일괄 실행 (Batch Run)**: 시나리오 100+개를 VM 풀(예: 6대)에서 비동기 병렬 실행, 자동 VM 할당/반환
- **실패 TC 자동 재시도**: 실패한 테스트를 스냅샷 롤백 후 자동으로 재시도 (MaxRetryCount 설정)
- **조건부 스텝 실행**: 이전 스텝 결과에 따라 실행 여부 결정
- **파일 전송**: 호스트 ↔ VM 간 파일 복사 지원
- **결과 수집 및 검증**: Exit Code, JSON Path, 텍스트 매칭 기반 성공 판정
- **네트워크 차단**: 오프라인 테스트를 위한 네트워크 강제 분리/복원
- **스크린샷 캡처**: 실행 중 주기적 스크린샷 자동 캡처
- **매크로 변수**: 경로/명령어에 `{DATE}`, `{VM_NAME}` 등 20+ 매크로 지원
- **스케줄러**: 일회/매일/매주/매월/간격 기반 자동 실행 예약
- **Pre/Post 이벤트**: 시나리오 실행 전후 스크립트/명령어 훅
- **알림**: Email, Slack, Teams 알림 연동
- **리포트**: HTML, JSON, XML(JUnit) 형식 결과 리포트 생성
- **CLI 지원**: CI/CD 파이프라인 연동을 위한 명령줄 인터페이스
- **비밀번호 보호**: DPAPI 암호화로 Guest 자격 증명 안전 저장

## 요구 사항

- Windows 10/11
- .NET Framework 4.7.2 이상
- VMware Workstation Pro
- VM 내 VMware Tools 설치 필요 (Guest 작업용)

## 설치

1. 저장소를 클론합니다.
2. Visual Studio 2019 이상에서 솔루션을 열고 빌드합니다.
3. `settings.example.json`을 `settings.json`으로 복사하고 환경에 맞게 수정합니다.

## 사용법

### GUI 모드

```bash
AutoRegressionVM.exe
```

#### 키보드 단축키

| 단축키 | 동작 |
|--------|------|
| `F5` | 시나리오 실행 |
| `Esc` | 실행 중지 |
| `Ctrl+E` | 시나리오 편집 |
| `Ctrl+N` | 새 시나리오 |
| `Ctrl+S` | 저장 |

### CLI 모드

```bash
# 시나리오 목록 조회
AutoRegressionVM.exe --list-scenarios

# VM 목록 조회
AutoRegressionVM.exe --list-vms

# 시나리오 실행
AutoRegressionVM.exe --scenario "테스트시나리오명"

# 특정 VM에서만 실행
AutoRegressionVM.exe --scenario "시나리오명" --vm "VM이름"

# 병렬 실행 (최대 2개 VM 동시 실행)
AutoRegressionVM.exe --scenario "시나리오명" --parallel 2

# 실행 제한 시간 설정 (분 단위)
AutoRegressionVM.exe --scenario "시나리오명" --timeout 60

# 출력 형식 지정 (text, json, xml)
AutoRegressionVM.exe --scenario "시나리오명" --output json
AutoRegressionVM.exe --scenario "시나리오명" --output xml

# 결과 리포트 저장
AutoRegressionVM.exe --scenario "시나리오명" --report "C:\Reports\result.json"

# 드라이런 (실제 실행 없이 검증)
AutoRegressionVM.exe --scenario "시나리오명" --dry-run

# 상세 로그 출력
AutoRegressionVM.exe --scenario "시나리오명" --verbose
```

#### CLI Exit Code

| 코드 | 의미 |
|------|------|
| `0` | 모든 테스트 성공 |
| `1` | 일부 테스트 실패 |
| `2` | 시나리오 또는 VM 없음 |
| `3` | VMware 연결 실패 |
| `4` | 잘못된 파라미터 |
| `5` | 타임아웃 또는 처리되지 않은 오류 |

## 테스트 시나리오 구성

시나리오는 여러 테스트 스텝으로 구성됩니다. 각 스텝에서는:

1. **스냅샷 복원**: 지정된 스냅샷으로 VM 상태 복원
2. **VMware Tools 대기**: Guest OS 부팅 완료까지 대기
3. **파일 복사**: 테스트에 필요한 파일을 VM으로 전송
4. **네트워크 차단**: 오프라인 테스트 시 네트워크 비활성화 (선택)
5. **테스트 실행**: VM 내에서 프로그램/스크립트 실행
6. **스크린샷 캡처**: 실행 중 주기적 스크린샷 저장 (선택)
7. **대기**: 실행 후 지정 시간 대기 (선택)
8. **네트워크 복원**: 차단된 네트워크 재활성화
9. **결과 수집**: VM에서 결과 파일을 호스트로 복사
10. **성공 판정**: Exit Code, JSON Path, 텍스트 기반 검증
11. **정리**: 스냅샷으로 VM 상태 복원 (선택)

### 실패 시 자동 재시도

시나리오에 `MaxRetryCount`를 설정하면, 실패한 TC를 자동으로 재시도합니다. 재시도 시 스냅샷을 롤백하여 깨끗한 상태에서 다시 실행합니다. 병렬 실행 중에도 개별 TC 단위로 재시도됩니다.

### 성공 기준 (SuccessCriteria)

| 기준 | 설명 |
|------|------|
| `ExpectedExitCode` | 기대하는 프로세스 종료 코드 (null이면 체크 안 함) |
| `ResultJsonPath` + `ExpectedJsonValue` | 결과 JSON 파일에서 특정 경로의 값 검증 |
| `ContainsText` | 출력에 반드시 포함되어야 할 문자열 |
| `NotContainsText` | 출력에 포함되면 안 되는 문자열 |

### 조건부 실행 (StepCondition)

| 조건 | 설명 |
|------|------|
| `Always` | 항상 실행 (기본값) |
| `PreviousStepPassed` | 바로 이전 스텝 성공 시 |
| `PreviousStepFailed` | 바로 이전 스텝 실패 시 |
| `SpecificStepResult` | 특정 스텝의 결과에 따라 |
| `AllPreviousPassed` | 모든 이전 스텝 성공 시 |
| `AnyPreviousFailed` | 이전 스텝 중 하나라도 실패 시 |

## 매크로 변수

경로, 명령어, 인자 등에서 `{MACRO}` 또는 `${MACRO}` 형식으로 사용할 수 있습니다.

| 매크로 | 설명 | 예시 |
|--------|------|------|
| `{DATE}` | 현재 날짜 | `20260312` |
| `{TIME}` | 현재 시간 | `143052` |
| `{DATETIME}` | 날짜+시간 | `20260312_143052` |
| `{TIMESTAMP}` | Unix 타임스탬프 | `1773496252` |
| `{YEAR}`, `{MONTH}`, `{DAY}` | 연/월/일 | `2026`, `03`, `12` |
| `{HOUR}`, `{MINUTE}`, `{SECOND}` | 시/분/초 | `14`, `30`, `52` |
| `{VM_NAME}` | 대상 VM 이름 | `Win10-QA` |
| `{VM_PATH}` | VM의 VMX 경로 | `D:\VMware\Win10\Win10.vmx` |
| `{STEP_NAME}` | 현재 스텝 이름 | `설치 테스트` |
| `{STEP_INDEX}` | 현재 스텝 인덱스 | `0` |
| `{SCENARIO_NAME}` | 시나리오 이름 | `회귀 테스트 v2` |
| `{SCENARIO_ID}` | 시나리오 ID | `a1b2c3d4-...` |
| `{USERNAME}` | Windows 사용자명 | `Developer` |
| `{MACHINE}` | 컴퓨터 이름 | `QA-PC-01` |
| `{TEMP}` | 임시 폴더 경로 | `C:\Users\...\Temp` |
| `{APPDIR}` | 애플리케이션 경로 | `D:\AutoRegressionVM\bin` |
| `{RESULT_DIR}` | 결과 저장 경로 | `...\Results\20260312\스텝명` |

**사용 예시:**
```
실행 파일: C:\Installers\{DATE}\setup.exe
결과 수집: {RESULT_DIR}\{VM_NAME}_result.json
```

## 스케줄러

GUI에서 ⏰ 버튼으로 스케줄러 다이얼로그를 열 수 있습니다.

| 유형 | 설명 |
|------|------|
| `Once` | 지정 시각에 한 번만 실행 |
| `Daily` | 매일 지정 시각에 실행 |
| `Weekly` | 매주 특정 요일/시각에 실행 |
| `Monthly` | 매월 특정 일/시각에 실행 |
| `Interval` | 지정 간격으로 반복 실행 |

스케줄 트리거 시 자동으로 VMware에 연결하고 지정된 시나리오를 실행합니다.

## 알림 설정

`settings.json`에서 알림 설정을 구성할 수 있습니다:

- **Email**: SMTP 서버, 포트, 인증 정보, 수신자 설정
- **Slack**: Incoming Webhook URL 설정
- **Teams**: Incoming Webhook URL 설정 (Adaptive Card 형식)

알림 트리거를 선택적으로 구성할 수 있습니다:
- `NotifyOnStart`: 테스트 시작 시
- `NotifyOnComplete`: 테스트 완료 시
- `NotifyOnFailure`: 테스트 실패 시
- `NotifyOnError`: 오류 발생 시

## 설정 파일

`settings.example.json`을 참고하여 `settings.json`을 작성합니다:

```json
{
  "VMwareInstallPath": "C:\\Program Files (x86)\\VMware\\VMware Workstation",
  "DefaultVMPath": "D:\\VMware",
  "ResultOutputPath": ".\\Results",
  "ScenariosPath": ".\\Scenarios",
  "Notification": {
    "Enabled": true,
    "Type": "Slack",
    "SlackWebhookUrl": "https://hooks.slack.com/services/YOUR/WEBHOOK/URL",
    "TeamsWebhookUrl": "",
    "SmtpServer": "",
    "SmtpPort": 587,
    "SmtpUsername": "",
    "SmtpPassword": "",
    "EmailTo": "",
    "NotifyOnComplete": true,
    "NotifyOnFailure": true,
    "NotifyOnStart": false,
    "NotifyOnError": true
  },
  "RegisteredVMs": []
}
```

## 프로젝트 구조

```
AutoRegressionVM/
├── CLI/                        # 명령줄 인터페이스
│   ├── CliOptions.cs           # CLI 옵션 모델
│   ├── CliRunner.cs            # CLI 실행 엔진
│   └── CommandLineParser.cs    # 인자 파서
├── Helpers/                    # 유틸리티 클래스
│   ├── CredentialProtector.cs  # DPAPI 비밀번호 암호화
│   ├── NullToCollapsedConverter.cs # XAML 값 변환기
│   ├── RelayCommand.cs         # ICommand 구현
│   ├── SimpleJson.cs           # JSON 유틸리티
│   └── ViewModelBase.cs        # MVVM 베이스 클래스
├── Models/                     # 데이터 모델
│   ├── AppSettings.cs          # 앱 설정
│   ├── BatchRunResult.cs       # 일괄 실행 결과 모델
│   ├── Snapshot.cs             # VM 스냅샷
│   ├── TestResult.cs           # 테스트 결과
│   ├── TestScenario.cs         # 시나리오 정의
│   ├── TestStep.cs             # 스텝/실행 정보/성공 기준
│   ├── VMExecutionStatus.cs    # VM별 실행 상태
│   └── VMInfo.cs               # VM 정보 (DPAPI 암호화)
├── Services/                   # 서비스 계층
│   ├── Notification/           # 알림 서비스
│   │   ├── INotificationService.cs
│   │   ├── NotificationManager.cs  # 팩토리 패턴 알림 관리
│   │   ├── EmailNotificationService.cs
│   │   ├── SlackNotificationService.cs
│   │   └── TeamsNotificationService.cs
│   ├── TestExecution/          # 테스트 실행 엔진
│   │   ├── IBatchRunnerService.cs  # 일괄 실행 인터페이스
│   │   ├── BatchRunnerService.cs   # VM 풀 기반 일괄 실행
│   │   ├── ITestRunner.cs
│   │   └── TestRunner.cs       # 순차/병렬 실행, 재시도
│   ├── VMware/                 # VMware 연동
│   │   ├── IVMwareService.cs
│   │   └── VixService.cs       # vmrun.exe 래퍼
│   ├── IReportService.cs
│   ├── IScenarioService.cs
│   ├── ISettingsService.cs
│   ├── MacroService.cs         # 매크로 변수 치환
│   ├── ReportService.cs        # HTML/JSON 리포트 생성
│   ├── ScenarioService.cs      # 시나리오 CRUD
│   ├── SchedulerService.cs     # 예약 실행 스케줄러
│   └── SettingsService.cs      # 설정 관리
├── ViewModels/                 # MVVM ViewModel
│   └── MainViewModel.cs
├── Views/                      # WPF 다이얼로그
│   ├── AddVMDialog.xaml        # VM 추가
│   ├── BatchRunDialog.xaml     # 일괄 실행 다이얼로그
│   ├── ScenarioEditorDialog.xaml # 시나리오 편집기
│   ├── SchedulerDialog.xaml    # 스케줄러 설정
│   ├── SettingsDialog.xaml     # 앱 설정
│   └── TestHistoryDialog.xaml  # 테스트 이력 조회
├── App.xaml                    # WPF 앱 루트
├── MainWindow.xaml             # 메인 윈도우
├── settings.example.json       # 설정 파일 예시
└── AutoRegressionVM.csproj     # 프로젝트 파일
```

## 아키텍처

- **패턴**: MVVM (Model-View-ViewModel)
- **DI**: 생성자 주입 (App.xaml.cs Composition Root)
- **SOLID**: 인터페이스 분리 (ISettingsService, IScenarioService, IReportService, ITestRunner, IVMwareService)
- **팩토리**: NotificationManager - 확장 가능한 알림 채널 등록
- **직렬화**: Newtonsoft.Json 통일 사용
- **보안**: DPAPI (CurrentUser scope) Guest 비밀번호 암호화

---

## 부록: QA 팀 활용 가이드

### A. 최초 설정 (첫 사용자용)

#### 1단계: 환경 준비

```
1. GitHub Releases에서 최신 ZIP 다운로드
2. 원하는 경로에 압축 해제 (예: D:\Tools\AutoRegressionVM)
3. settings.example.json → settings.json으로 복사
4. settings.json을 열어 환경에 맞게 수정:
```

```json
{
  "VMwareInstallPath": "C:\\Program Files (x86)\\VMware\\VMware Workstation",
  "DefaultVMPath": "D:\\VMware",
  "ResultOutputPath": ".\\Results",
  "ScenariosPath": ".\\Scenarios"
}
```

#### 2단계: VM 준비

```
1. VMware Workstation에서 테스트용 VM 생성
2. VM 내에 VMware Tools 설치 (필수 — Guest 파일 복사/실행에 필요)
3. VM의 Guest OS에 로그인 계정 확인 (예: Administrator / P@ssw0rd)
4. 테스트 전 기준이 되는 "깨끗한 상태" 스냅샷 생성 (예: "Clean")
```

#### 3단계: 프로그램 시작

```
1. AutoRegressionVM.exe 실행
2. 좌측 상단 "연결" 버튼 클릭 → VMware에 연결
3. "VM 추가" 버튼으로 테스트 대상 VM 등록
   - VM 이름, VMX 경로, Guest 계정/비밀번호 입력
4. Ctrl+N으로 새 시나리오 생성
```

---

### B. GUI 화면 구성

프로그램 실행 시 메인 윈도우는 다음과 같이 구성됩니다:

```
┌─────────────────────────────────────────────────────────────────────┐
│ 툴바: [연결] [연결해제] | [VM추가] [새시나리오] | [실행] [중지]     │
│       [재실행] [일괄실행] | [설정] | [스케줄] [이력] [리포트]       │
├──────────┬──────────────────────────────┬───────────────────────────┤
│ VM 목록  │     테스트 시나리오           │     테스트 결과           │
│          │                              │                           │
│ ● Win10  │ [시나리오 선택 ▼]            │ ✅ 설치 테스트            │
│ ○ Win11  │  [✏복제📋📥📤🗑]           │    Win10-QA  00:45        │
│          │                              │ ❌ 기능 테스트            │
│ ── 실행중 ──│ 병렬VM: 2  대상VM: 3개     │    Win11-QA  01:23        │
│ ● Win10  │                              │    오류: Exit code 1      │
│  복사중.. │ 테스트 Steps:                │ ⏭ 정리 (Skipped)         │
│  ████░░  │ #0 설치 테스트               │                           │
│ ● Win11  │    스냅샷: Clean             │                           │
│  실행중.. │    실행: setup.exe           │                           │
│  █████░  │ #1 기능 테스트               │                           │
│          │    스냅샷: Installed          │                           │
├──────────┴──────────────────────────────┴───────────────────────────┤
│ [████████████████████████░░░░] 파일 복사 중...  68%    00:02:15     │
├────────────────────────────────────────────────────────────────────-┤
│ 로그                                                    [💾] [🗑]  │
│ [09:15:30] [INFO] Win10-QA: 스냅샷 "Clean" 복원 시작               │
│ [09:15:45] [INFO] Win10-QA: VMware Tools 대기 중...                │
│ [09:16:02] [INFO] Win10-QA: setup.exe 복사 중...                   │
├────────────────────────────────────────────────────────────────────-┤
│ ● 연결됨 | VM: 3대 | 시나리오: 5개 | F5:실행  Esc:중지  Ctrl+E:편집│
└─────────────────────────────────────────────────────────────────────┘
```

| 영역 | 설명 |
|------|------|
| **툴바** | VMware 연결, VM/시나리오 관리, 실행/중지/일괄실행, 설정/스케줄/이력/리포트 |
| **VM 목록** (좌측) | 등록된 VM 표시. 녹색 ●=켜짐, 회색 ○=꺼짐. 실행 중에는 VM별 진행 상태 표시 |
| **시나리오** (중앙) | 드롭다운으로 시나리오 선택. 스텝 목록과 설정 요약 표시. 우클릭으로 편집/복제/내보내기/가져오기/삭제 |
| **테스트 결과** (우측) | 스텝별 결과 (✅성공, ❌실패, ⏭스킵, ⚠오류). 마우스 올리면 상세 오류/출력 확인 |
| **진행률 바** | 현재 단계, 진행률 %, 경과 시간 표시 |
| **로그** | 실시간 로그 출력 (💾 내보내기, 🗑 초기화) |
| **상태 표시줄** | 연결 상태, VM 수, 시나리오 수, 단축키 안내 |

---

### C. GUI 단계별 사용법

#### 워크플로우 1: VMware 연결 및 VM 등록

```
1. 툴바에서 [🔌 연결] 클릭
   → 상태 표시줄이 "● 연결됨"으로 변경되면 성공
   → 실패 시: [⚙ 설정] → "일반" 탭 → VMware 설치 경로 확인

2. 툴바에서 [➕ VM 추가] 클릭
   → "VM 추가" 다이얼로그가 열림:
   ┌─────────────────────────────┐
   │ VM 이름:   [Win10-QA      ]│
   │ VMX 경로:  [D:\VMware\...] │ [찾아보기]
   │ 사용자명:  [Administrator  ]│
   │ 비밀번호:  [••••••••      ]│
   │              [추가] [취소]  │
   └─────────────────────────────┘
   - "찾아보기"로 .vmx 파일 선택
   - 사용자명/비밀번호: VM 안의 Guest OS 로그인 계정
   - 비밀번호는 DPAPI로 자동 암호화 저장

3. 또는 [⚙ 설정] → "VM 관리" 탭 → [VMware에서 가져오기]
   → VMware에 등록된 모든 VM을 자동으로 불러옴
   → DataGrid에서 사용자명/비밀번호를 셀 더블클릭하여 편집
```

---

#### 워크플로우 2: 시나리오 만들기 (시나리오 편집기)

```
1. 툴바에서 [📝 새 시나리오] 또는 Ctrl+N
   → 시나리오 편집기 다이얼로그가 열림
```

시나리오 편집기는 **상단 탭**과 **하단 스텝 편집 영역**으로 구분됩니다:

**상단: 시나리오 설정 (4개 탭)**

```
┌─────────────────────────────────────────────────────────────┐
│ [기본 정보] [테스트 전 이벤트] [테스트 후 이벤트] [대상VM/파일분배] │
├─────────────────────────────────────────────────────────────┤
│ 이름: [설치 테스트 v2.5        ]  병렬 VM: [3 ]             │
│ 설명: [v2.5.0 빌드 설치 검증   ]                            │
│ ☑ 실패 시 계속 진행                                         │
└─────────────────────────────────────────────────────────────┘
```

| 탭 | 설정 항목 |
|-----|----------|
| **기본 정보** | 이름, 설명, 병렬 VM 수, "실패 시 계속 진행" 체크 |
| **테스트 전 이벤트** | 테스트 시작 전 실행할 스크립트 (유형: 명령줄/PowerShell/배치/실행파일, 타임아웃, 실패 시 중단 여부) |
| **테스트 후 이벤트** | 테스트 완료 후 실행할 스크립트 (실행 조건: 항상/성공 시/실패 시) |
| **대상 VM / 파일 분배** | 병렬 실행할 VM 체크 선택 + 테스트 파일 목록 (VM별 자동 분배) |

**하단: 스텝 편집**

```
┌──────────────┬──────────────────────────────────────────────┐
│ 테스트 스텝  │ 스텝 상세 편집                                │
│              │                                              │
│ #0 설치      │ ┌ 스텝 정보 ──────────────────────────┐      │
│ #1 기능 테스트│ │ 스텝 이름: [설치 파일 실행         ] │      │
│ #2 정리      │ │ 대상 VM:   [Win10-QA           ▼]  │      │
│              │ │ 스냅샷:    [Clean              ▼] [↻]│     │
│ [추가][복제] │ └──────────────────────────────────────┘      │
│ [삭제][▲][▼]│                                              │
│              │ ┌ 파일 복사 (호스트 → VM) ────────────┐      │
│              │ │ 소스 경로(호스트)  │ 대상 경로(VM)   │      │
│              │ │ D:\Builds\setup.exe│ C:\Temp\setup.exe│     │
│              │ │        [파일 추가] [폴더 추가] [삭제]│      │
│              │ └──────────────────────────────────────┘      │
│              │                                              │
│              │ ┌ 실행 설정 ──────────────────────────┐      │
│              │ │ 실행 유형: [Program          ▼]     │      │
│              │ │ 실행 경로: [C:\Temp\setup.exe ] [..]│      │
│              │ │ 인수:      [/S /NORESTART        ]  │      │
│              │ │ 타임아웃:  [300                  ]초 │      │
│              │ └──────────────────────────────────────┘      │
│              │                                              │
│              │ ┌ 실행 후 대기 ────────────────────────┐     │
│              │ │ [0]시간 [0]분 [30]초                  │     │
│              │ │ 빠른설정: [없음][30초][1분][5분][10분]│     │
│              │ └──────────────────────────────────────┘      │
│              │                                              │
│              │ ┌ 결과 파일 수집 (VM → 호스트) ────────┐     │
│              │ │ VM 내 경로        │ 호스트 저장 경로   │     │
│              │ │ C:\Temp\result.json│ {RESULT_DIR}\...  │     │
│              │ │          [추가] [삭제] [대상 폴더 선택]│     │
│              │ └──────────────────────────────────────┘      │
│              │                                              │
│              │ ┌ 실행 조건 ──────────────────────────┐      │
│              │ │ [항상 실행                       ▼]  │      │
│              │ └──────────────────────────────────────┘      │
│              │                                              │
│              │ ┌ 옵션 ───────────────────────────────┐      │
│              │ │ ☑ 네트워크 강제 분리                  │      │
│              │ │ ☑ 완료 후 스냅샷 복원                 │      │
│              │ │ ☐ 스크린샷 캡처  간격: [10]초         │      │
│              │ └──────────────────────────────────────┘      │
│              │                                              │
│              │                         [저장] [취소]        │
└──────────────┴──────────────────────────────────────────────┘
```

**스텝 편집 핵심 포인트:**

| 영역 | 조작 방법 |
|------|----------|
| **스텝 목록** | [추가]로 새 스텝, [복제]로 복사, [▲][▼]로 순서 변경, 드래그&드롭 지원, Delete 키로 삭제 |
| **대상 VM/스냅샷** | 드롭다운에서 선택. [↻]로 스냅샷 목록 새로고침 |
| **파일 복사** | [파일 추가]로 개별 파일, [폴더 추가]로 폴더 선택. 경로는 셀 직접 편집 가능 |
| **실행 설정** | Program(exe), Script(bat/ps1), Command(cmd /c) 중 선택. [..]로 파일 찾기 |
| **실행 후 대기** | 빠른설정 버튼으로 원클릭 지정 (없음~1시간), 또는 시/분/초 직접 입력 |
| **결과 수집** | VM에서 호스트로 가져올 파일 경로 지정. `{RESULT_DIR}`, `{VM_NAME}` 등 매크로 사용 가능 |
| **실행 조건** | "항상 실행", "이전 스텝 성공 시", "이전 스텝 실패 시", "특정 스텝 결과에 따라", "모든 이전 성공 시", "하나라도 실패 시" |
| **옵션** | 네트워크 차단, 스냅샷 복원, 스크린샷 캡처 + 간격 설정 |

---

#### 워크플로우 3: 시나리오 실행 및 모니터링

```
1. 중앙 패널 드롭다운에서 실행할 시나리오 선택
2. [▶ 실행] 클릭 또는 F5
   → 좌측 VM 목록 하단에 VM별 실행 상태가 나타남:
     ● Win10-QA  "파일 복사 중"  ████░░░░
     ● Win11-QA  "테스트 실행 중"  ██████░░
   → 진행률 바에 현재 단계와 % 표시
   → 로그 영역에 실시간 로그 출력

3. 실행 중지: [⏹ 중지] 클릭 또는 Esc

4. 실행 완료 후:
   → 우측 "테스트 결과" 패널에 스텝별 결과 표시
     ✅ = 성공, ❌ = 실패, ⏭ = 스킵, ⚠ = 오류, 🔄 = 실행중
   → 실패한 항목에 마우스를 올리면 오류 메시지, 출력, Exit Code 확인
   → [🔁 재실행]으로 같은 시나리오 다시 실행

5. 로그 관리:
   → [💾] 버튼: 로그를 파일로 내보내기
   → [🗑] 버튼: 로그 초기화
```

---

#### 워크플로우 4: 설정 변경

```
툴바에서 [⚙ 설정] 클릭 → 3개 탭으로 구성:
```

**탭 1: 일반**
```
┌ VMware 설정 ──────────────────────────────────────┐
│ VMware 설치 경로: [C:\...\VMware Workstation] [찾아보기]│
└───────────────────────────────────────────────────┘
┌ 저장 경로 ────────────────────────────────────────┐
│ 시나리오 저장: [.\Scenarios] [찾아보기]              │
│ 결과 저장:    [.\Results  ] [찾아보기]              │
└───────────────────────────────────────────────────┘
```

**탭 2: 알림**
```
☑ 알림 사용

┌ 알림 설정 ────────────────────────────────────────┐
│ 알림 유형: [Slack ▼]  (Slack / Microsoft Teams / Email)│
│                                                    │
│ < Slack 선택 시 >                                   │
│ Webhook URL: [https://hooks.slack.com/services/...]│
│                                                    │
│ < Teams 선택 시 >                                   │
│ Webhook URL: [https://outlook.office.com/webhook/...]│
│                                                    │
│ < Email 선택 시 >                                   │
│ SMTP 서버: [smtp.gmail.com    ] 포트: [587]        │
│ 사용자명:  [qa-team@company.com]                    │
│ 비밀번호:  [••••••••          ]                     │
│ 받는 사람: [team@company.com  ] (쉼표로 다수 지정)  │
│                                                    │
│ [연결 테스트]  ← 설정 확인용                         │
└───────────────────────────────────────────────────┘
┌ 알림 조건 ────────────────────────────────────────┐
│ ☐ 테스트 시작 시 알림                               │
│ ☑ 테스트 완료 시 알림                               │
│ ☑ 테스트 실패 시 알림                               │
│ ☑ 오류 발생 시 알림                                 │
└───────────────────────────────────────────────────┘
```

**탭 3: VM 관리**
```
[VMware에서 가져오기] [VM 추가] [VM 제거]

┌─────────────────────────────────────────────────┐
│ 이름      │ VMX 경로              │사용자명│비밀번호│
│ Win10-QA  │ D:\VMware\Win10\...   │Admin  │••••  │
│ Win11-QA  │ D:\VMware\Win11\...   │Admin  │••••  │
│ WinSvr    │ D:\VMware\WinSvr\...  │Admin  │••••  │
└─────────────────────────────────────────────────┘
※ 셀을 더블클릭하여 편집
```

---

#### 워크플로우 5: 일괄 실행 (Batch Run)

> **상황**: 시나리오 100+개를 VM 6대로 병렬 실행해야 할 때

```
1. 툴바에서 [📦 일괄실행] 클릭 (주황색 버튼)
   → 일괄 실행 다이얼로그가 열림:

┌─────────────────────────────────────────────────────────────┐
│ VM 풀 설정                                                   │
│ 동시 실행 VM 수: [4] 대    등록된 VM: 6대  [전체선택][전체해제]│
├───────────────────────────────────┬───────────────────────────┤
│ 실행할 시나리오                    │ VM 풀                     │
│ [전체 선택] [전체 해제]  선택: 5개 │                           │
│                                    │ ☑ Win10-QA               │
│ ☑ 설치 테스트 v2.5                │    D:\VMware\Win10\...    │
│ ☑ 기능 테스트 A                   │ ☑ Win11-QA               │
│ ☑ 기능 테스트 B                   │    D:\VMware\Win11\...    │
│ ☑ 업그레이드 테스트               │ ☑ WinSvr                 │
│ ☑ 제거 테스트                     │    D:\VMware\WinSvr\...   │
│ ☐ 성능 테스트                     │ ☑ Win10-Dev              │
│                                    │ ☑ Win11-Dev              │
│                                    │ ☑ WinSvr-Dev             │
├────────────────────────────────────┴───────────────────────────┤
│ [████████████████░░░░░░░░] 3 / 5                  00:05:23   │
├───────────────────────────────────────────────────────────────┤
│ 실행 상태:                                                    │
│ ✅ 설치 테스트 v2.5          Win10-QA        성공             │
│ ✅ 기능 테스트 A             Win11-QA        성공             │
│ 🔄 기능 테스트 B             WinSvr          실행중           │
│ 🔄 업그레이드 테스트         Win10-Dev       실행중           │
│ ⏳ 제거 테스트                               대기             │
├───────────────────────────────────────────────────────────────┤
│                              [일괄 실행] [취소] [닫기]        │
└───────────────────────────────────────────────────────────────┘

2. 왼쪽에서 실행할 시나리오 체크 (다중 선택)
3. 오른쪽에서 VM 풀로 사용할 VM 체크
4. "동시 실행 VM 수" 설정 (하드웨어 리소스에 따라 조절)
5. [일괄 실행] 클릭
   → VM 풀에서 시나리오가 비동기 병렬로 실행
   → 시나리오 완료 시 VM이 풀에 반환 → 대기 중인 다음 시나리오에 자동 할당
   → 실시간으로 시나리오별 상태/할당 VM 표시
6. 취소: [취소] → 실행 중인 시나리오는 완료 후 중지
7. 완료 시 결과 요약 팝업 (총/성공/실패/소요시간)
```

**핵심 동작 원리:**
- `ConcurrentQueue`로 VM 풀 관리 — 시나리오 완료 시 VM 자동 반환/재할당
- `SemaphoreSlim`으로 동시 실행 수 제어 — 하드웨어 리소스 보호
- 시나리오당 독립 TestRunner 인스턴스 — 상태 격리로 안전한 병렬 실행
- 시나리오에 대상 VM이 지정되어 있으면 해당 VM 사용, 없으면 풀에서 자동 할당

---

#### 워크플로우 6: 스케줄러 (예약 실행)


```
툴바에서 [⏰ 스케줄] 클릭:

┌─────────────────────────────┬──────────────────┐
│ 예약된 작업                  │ 스케줄 설정       │
│                              │                  │
│ ☑ 야간회귀  회귀테스트 Daily │ 이름:   [야간회귀]│
│   03-13 02:00               │ 시나리오:[회귀▼]  │
│ ☐ 주간빌드  빌드검증 Weekly  │ 유형:   [매일 ▼] │
│   03-15 09:00               │ 실행시간:[02]:[00]│
│                              │                  │
│         [추가] [삭제]        │ ☑ 활성화         │
│                              │         [저장]   │
└─────────────────────────────┴──────────────────┘

유형별 추가 옵션:
- "매주" 선택 시 → 요일 선택 드롭다운 (일~토)
- "매월" 선택 시 → 일자 입력 (1~31)
- "간격" 선택 시 → 반복 간격 설정
```

**사용 시 주의:** 프로그램이 켜져 있어야 스케줄이 동작합니다.

---

#### 워크플로우 7: 결과 확인 및 리포트

```
실행 완료 후 활용 방법:

1. 테스트 결과 패널 (우측)
   → 각 항목 마우스 오버 시 툴팁으로 상세 정보 확인:
     - 스텝 이름, 오류 메시지, 프로그램 출력, Exit Code

2. [📊 이력] 버튼
   → 과거 실행 기록 조회
   → 날짜/시나리오별 결과 확인

3. [📄 리포트] 버튼
   → HTML 리포트 자동 생성 (브라우저에서 열기 가능)
   → 색상 코딩, 접히는 섹션, 스크린샷 포함
   → 공유 폴더나 이메일로 전달 가능

4. 로그 [💾] 버튼
   → 전체 실행 로그를 텍스트 파일로 저장
```

---

#### 워크플로우 8: 시나리오 공유 (내보내기/가져오기)

```
다른 팀원에게 시나리오를 공유하려면:

1. 내보내기: 중앙 패널에서 시나리오 우클릭 → "내보내기"
   또는 [📤] 버튼 클릭 → JSON 파일로 저장

2. 가져오기: [📥] 버튼 클릭 → JSON 파일 선택
   또는 시나리오 우클릭 → "가져오기"

3. 복제: [📋] 버튼 클릭 → 선택한 시나리오의 사본 생성
```

---

### D. 활용 케이스 (GUI 기반)

#### 케이스 1: 단일 VM 설치 테스트

> **상황**: 신규 빌드 setup.exe가 정상 설치되는지 확인

```
1. [📝 새 시나리오] 클릭
2. 기본 정보 탭:
   - 이름: "설치 테스트 v2.5.0"
   - 병렬 VM: 1
3. 스텝 [추가] 클릭
4. 스텝 편집:
   - 스텝 이름: "설치 실행"
   - 대상 VM: Win10-QA 선택
   - 스냅샷: Clean 선택
   - 파일 복사: [파일 추가] → D:\Builds\setup.exe → C:\Temp\setup.exe
   - 실행 유형: Program
   - 실행 경로: C:\Temp\setup.exe
   - 인수: /S /NORESTART
   - 타임아웃: 300
   - ☑ 완료 후 스냅샷 복원
   - ☐ 네트워크 강제 분리 (해제)
5. [저장]
6. F5로 실행
```

---

#### 케이스 2: 다중 VM 병렬 테스트

> **상황**: Win10, Win11, WinServer에서 동시에 테스트

```
1. 시나리오 편집기 열기
2. 기본 정보 탭:
   - 병렬 VM: 3
   - ☑ 실패 시 계속 진행
3. "대상 VM / 파일 분배" 탭:
   - Win10-QA  ☑
   - Win11-QA  ☑
   - WinSvr    ☑
4. 스텝 추가 후 설정 (VM은 공통 스텝 사용)
5. 결과 수집 경로에 {VM_NAME} 매크로 사용:
   - VM 내 경로: C:\Temp\result.json
   - 호스트 경로: {RESULT_DIR}\{VM_NAME}_result.json
6. [저장] 후 실행
   → 좌측에 3개 VM의 실시간 진행 상태가 동시에 표시됨
```

---

#### 케이스 3: 오프라인 테스트 (네트워크 차단)

> **상황**: 인터넷 없이 제품이 정상 동작하는지 확인

```
1. 스텝 편집에서:
   - ☑ 네트워크 강제 분리  ← 체크
   - ☑ 스크린샷 캡처, 간격: 15초
2. 실행하면:
   파일 복사 → 네트워크 차단 → 프로그램 실행 → 네트워크 복원 → 결과 수집
   (자동으로 차단/복원 처리)
```

---

#### 케이스 4: 조건부 실행 (설치 → 테스트 → 실패 시 로그)

> **상황**: 설치 성공 시에만 테스트, 실패하면 디버그 로그 수집

```
1. 스텝 3개 추가:

   스텝 #0 "제품 설치":
   - 실행 조건: 항상 실행
   - ☐ 완료 후 스냅샷 복원 (해제 — 설치 상태 유지)

   스텝 #1 "기능 테스트":
   - 실행 조건: "이전 스텝 성공 시" 선택
   - ☐ 완료 후 스냅샷 복원 (해제)

   스텝 #2 "로그 수집":
   - 실행 조건: "하나라도 실패 시" 선택
   - 결과 수집: C:\ProgramData\MyProduct\logs\*.log
   - ☑ 완료 후 스냅샷 복원
```

---

#### 케이스 5: Pre/Post 이벤트 활용

> **상황**: 실행 전 빌드 서버에서 다운로드, 실행 후 결과를 공유 폴더로 복사

```
1. 시나리오 편집기 → "테스트 전 이벤트" 탭:
   - ☑ 테스트 전 이벤트 활성화
   - 유형: PowerShell
   - 명령: Invoke-WebRequest -Uri 'http://build/latest/setup.exe' ...
   - ☑ 실패 시 테스트 중단

2. "테스트 후 이벤트" 탭:
   - ☑ 테스트 후 이벤트 활성화
   - 유형: 배치 파일
   - 명령: xcopy /Y /S .\Results\* \\file-server\QA\Results\
   - 실행 조건: "항상 실행"
```

---

#### 케이스 6: 야간 자동 실행 + Slack 알림

> **상황**: 매일 새벽 2시 자동 회귀 테스트, 출근 시 Slack으로 결과 확인

```
1. [⚙ 설정] → "알림" 탭:
   - ☑ 알림 사용
   - 유형: Slack
   - Webhook URL 입력
   - [연결 테스트]로 확인
   - ☑ 테스트 시작 시 / ☑ 완료 시 / ☑ 실패 시

2. [⏰ 스케줄] 클릭:
   - [추가]
   - 이름: "야간 회귀"
   - 시나리오: "전체 회귀 테스트" 선택
   - 유형: "매일"
   - 실행 시간: 02:00
   - ☑ 활성화
   - [저장]

3. 프로그램을 켜둔 상태로 유지
   → 새벽 2시에 자동 실행 → Slack으로 결과 알림
```

---

#### 케이스 7: 대규모 시나리오 일괄 실행

> **상황**: 릴리즈 전 100개 시나리오를 VM 6대로 병렬 실행

```
1. 모든 시나리오를 미리 GUI에서 작성해 둠
2. [📦 일괄실행] 클릭
3. [전체 선택]으로 100개 시나리오 모두 선택
4. VM 풀에서 6대 모두 체크
5. 동시 실행 VM 수: 6 입력
6. [일괄 실행] 클릭
   → 6개 시나리오가 동시에 시작
   → 하나가 끝나면 해당 VM에 다음 대기 시나리오 자동 할당
   → 100개 모두 완료될 때까지 반복
   → 실시간으로 각 시나리오의 상태 확인 가능
   → 하드웨어 부담 시 동시 실행 수를 4→3으로 줄여서 재실행
```

---

#### 케이스 8: CI/CD 연동 (CLI 모드)

> **상황**: Jenkins/GitHub Actions에서 빌드 후 자동 VM 테스트

GUI에서 시나리오를 미리 만들어 두고, CI/CD에서는 CLI로 호출합니다:

```bash
# 기본 실행
AutoRegressionVM.exe --scenario "회귀 테스트" --parallel 3

# JUnit XML 결과 + 타임아웃 설정
AutoRegressionVM.exe --scenario "회귀 테스트" --output xml --report results.xml --timeout 120

# 특정 VM만 실행
AutoRegressionVM.exe --scenario "회귀 테스트" --vm "Win10-QA"

# 드라이런 (실제 실행 없이 검증)
AutoRegressionVM.exe --scenario "회귀 테스트" --dry-run
```

**Jenkins Pipeline 예시:**
```groovy
stage('VM Regression Test') {
    steps {
        bat 'AutoRegressionVM.exe --scenario "회귀 테스트" --output xml --report results.xml --timeout 120 --verbose'
    }
    post { always { junit 'results.xml' } }
}
```

**Exit Code로 배포 결정:**

| 코드 | 의미 | CI/CD 대응 |
|------|------|-----------|
| `0` | 모든 테스트 성공 | 배포 진행 |
| `1` | 일부 실패 | 배포 중단, 리포트 확인 |
| `5` | 타임아웃 | 재시도 또는 타임아웃 증가 |

---

### C. 자주 묻는 질문 (FAQ)

**Q: VM이 부팅되지 않아 테스트가 실패합니다.**
> VMware Tools가 Guest OS에 설치되어 있는지 확인하세요. 스냅샷 복원 후 VMware Tools가 "running" 상태가 되어야 파일 복사 및 프로그램 실행이 가능합니다. 기본 대기 시간은 300초입니다.

**Q: 병렬 실행 시 하나의 VM이 실패하면 나머지도 멈추나요?**
> 아닙니다. 각 VM은 독립적으로 실행됩니다. 한 VM이 실패해도 다른 VM은 계속 진행합니다. `ContinueOnFailure: true`이면 실패한 VM의 나머지 스텝도 계속 실행합니다.

**Q: 실패한 TC는 어떻게 재시도되나요?**
> `MaxRetryCount` 설정값만큼 자동 재시도합니다. 재시도 시 스냅샷을 롤백하여 깨끗한 상태에서 처음부터 다시 실행합니다. 재시도 사이에 3초 대기합니다.

**Q: Guest 비밀번호는 안전한가요?**
> 비밀번호는 Windows DPAPI (CurrentUser scope)로 암호화되어 settings.json에 저장됩니다. 같은 Windows 계정에서만 복호화할 수 있으므로, 다른 사용자가 파일을 복사해도 비밀번호를 알 수 없습니다.

**Q: 테스트 결과를 팀과 공유하려면?**
> 세 가지 방법이 있습니다:
> 1. **알림**: Slack/Teams/Email로 실시간 결과 수신
> 2. **리포트**: HTML 리포트 생성 후 공유 폴더에 저장
> 3. **PostTestEvent**: 테스트 완료 후 결과를 자동으로 공유 폴더에 복사

**Q: CLI에서 특정 VM만 테스트하고 싶습니다.**
> `--vm` 옵션을 사용하세요:
> ```bash
> AutoRegressionVM.exe --scenario "회귀 테스트" --vm "Win10-QA"
> ```

**Q: settings.json을 다른 PC로 옮기면 비밀번호가 깨집니다.**
> DPAPI는 Windows 사용자 계정에 종속됩니다. 다른 PC에서는 VM을 다시 등록하고 비밀번호를 재입력해야 합니다. 기존 평문 비밀번호가 있으면 자동으로 암호화 마이그레이션됩니다.

---

### D. 트러블슈팅

| 증상 | 원인 | 해결 |
|------|------|------|
| "VMware 연결 실패" | vmrun.exe 경로 불일치 | settings.json의 `VMwareInstallPath` 확인 |
| "스냅샷 복원 실패" | 스냅샷 이름 불일치 | VM의 스냅샷 이름과 시나리오 설정 비교 |
| "파일 복사 실패" | VMware Tools 미설치/미실행 | Guest OS에서 VMware Tools 상태 확인 |
| "프로그램 실행 타임아웃" | TimeoutSeconds 부족 | 스텝의 `TimeoutSeconds` 값 증가 |
| "Guest 로그인 실패" | 자격 증명 불일치 | VM 설정에서 계정/비밀번호 재확인 |
| "네트워크 차단 후 복원 안됨" | wmic 명령 권한 부족 | Guest OS에서 관리자 계정 사용 |
| CLI에서 출력 없음 | Console 연결 실패 | 명령 프롬프트에서 직접 실행 (PowerShell ISE 제외) |

## 라이선스

MIT License
